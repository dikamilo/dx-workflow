# Scripts

## Language choice
Scripts in `/scripts` are written as `.mjs` (ESM Node) or pure `.sh` (POSIX shell) — no `.js`/CommonJS, no other interpreters (Python, Ruby, etc.) unless the task genuinely can't be done in either.
